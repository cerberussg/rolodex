<nav class="navbar navbar-fixed-top brand-primary">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar-collapse" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>

      <%= link_to "Rolodex", root_path, class: "navbar-brand text-uppercase" %>
    </div>
    <!-- /.navbar-header -->
    <div class="collapse navbar-collapse" id="navbar-collapse">
      <% if !user_signed_in? %>
        <div class="navbar-right">
          <%= link_to "Login", new_user_session_path, class: "btn btn-default navbar-btn" %>
          <%= link_to "Register", new_user_registration_path, class: "btn btn-primary navbar-btn" %>
        </div>
      <% else %>
        <ul class="nav navbar-nav">
          <li class="<%= active_item('dashboard') %>"><%= link_to "Dashboard", dashboard_path, class: "brand-primary" %></li>
          <li class="<%= active_item('contacts') %>"><%= link_to "All Contacts", contacts_path, class: "brand-primary" %></li>
          <li><a href="#" class="brand-primary">Integrations</a></li>
        </ul>
        <ul class="nav navbar-right navbar-nav">
          <li class="dropdown">
            <a href="#" class="dropdown-toggle brand-primary" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
              <i class="glyphicon glyphicon-user"></i>
              <%= current_user.name %>
              <span class="caret"></span>
            </a>
            <ul class="dropdown-menu brand-primary" aria-labelledby="dropdownMenu1">
              <li><%= link_to "Profile", edit_user_registration_path, class: 'brand-primary' %></li>
              <li>
                <%= link_to "Logout", destroy_user_session_path, method: :delete, class: 'brand-primary' %>
              </li>
            </ul>
          </li>
        </ul>
          <%= render 'layouts/search_form' %>
      <% end %>
    </div>
  </div>
</nav>
